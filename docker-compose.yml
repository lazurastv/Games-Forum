version: "3"
services:
  backend:
    build: backend
    ports:
      - 8080:8080
  nginx:
    image: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/user:/usr/src/app/user
      - ./nginx/content:/usr/src/app/content
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
  frontend:
    build: frontend
    ports:
      - 3000:3000
